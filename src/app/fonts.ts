import { Changa, Inter, Orbitron } from "next/font/google";





export const inter = Inter(
    { subsets: ["latin"] }
);
export const changa = Changa(
    {subsets : ["arabic"]}
)

export const orbitron = Orbitron({
    subsets : ["latin"], 
    weight : "500"
})
